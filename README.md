# 資產槓桿現金流

「投資資產 × 股票質押 × 現金流 × 被動收入」模擬 APP。這是一套手機優先、離線優先的個人投資資產負債表，讓使用者先建立股票與現金基準線，再逐步加入質押、槓桿風控、現金流與被動收入目標規劃。

## V0.1 已完成

- React + TypeScript + Vite
- PWA manifest、Service Worker 與 SVG APP icon
- 手機 Bottom Navigation、桌面 Sidebar、Responsive UI
- IndexedDB 本機資料庫與 schema version 預留
- 0050、00878 與 NT$1,000,000 Demo 資料（可清除）
- 股票與現金資產新增、編輯、刪除
- 總資產、淨資產、股票市值、現金、未實現損益與月股息預估
- Light / Dark / System 顯示模式
- 財務公式獨立於 UI，並有 Vitest 單元測試
- GitHub Actions 型別檢查、測試與 GitHub Pages 部署設定

V0.1 的基線不把借款當成已存在的資產；V0.2 已加入實際借款與維持率管理，V0.3 再加入不寫入資料庫的借款再投入試算，V0.4 加入市場壓力測試，V0.5 接上每月現金流管理，V0.6 把被動收入目標換算成所需本金與股數，V0.7 再把多個借款／投資方案放在同一張比較表，V1.0 補齊本機 JSON 備份與完整手機使用流程，V1.1 接上股票公開行情查詢與手動備援，V1.2 加入房產、一般負債與家庭固定支出。這能確保「總資產」與「淨資產」不被借款誤算混淆。

## V0.2 已完成

- 質押借款新增、編輯、刪除與 IndexedDB bundle 儲存
- 借款本金、剩餘本金（目前尚欠）、年利率、還款方式、借款日與到期日
- 股票擔保品複選、質押股數與擔保品市值預覽
- 每月利息估算、維持率、警戒線、追繳線與距離門檻
- 首頁資產健康度與質押風控總覽接上真實借款資料
- 安全 / 警戒 / 追繳風險狀態與手機版表單檢查

V0.2 仍是手動輸入的模擬工具，不代表任何金融機構的正式授信、維持率或追繳通知。壓力測試與現金流項目會在後續版本加入。

## V0.3 已完成

- 借款金額 Slider 與數字輸入、年利率、借款期間與還款方式
- 模擬擔保品選擇與質押股數調整
- 借款投入指定股票、投入比例、可買股數與新增股票市值
- 新增年度股息、年度 / 月利息、模擬後現金與月淨現金流
- Before / After 比較總資產、總負債、淨資產、負債比與資產槓桿
- 模擬方案只在畫面暫存，不會改動 IndexedDB 的實際借款資料

V0.3 仍以起始價格的靜態試算為主；V0.4 已加入市場下跌壓力測試。

## V0.4 已完成

- 市場跌幅快速情境：-5%、-10%、-20%、-30%、-40%、-50%
- 0%–100% 自訂跌幅 Slider 與數字輸入
- 壓力後股票市值、擔保品市值、總資產、淨資產與負債比
- 壓力後維持率、安全／警戒／追繳風險與距離警戒線
- 反推「跌多少會碰到警戒線／追繳線」
- 所有壓力測試只在畫面試算，不會改動 IndexedDB 實際資料

V0.4 假設股票與擔保品同步下跌，現金、剩餘借款本金與利率維持不變；實際金融機構仍可能依個別股票折算率、集中度與契約條件判定。

## V0.5 已完成

- 收入項目管理：薪資、租金、利息收入與其他收入
- 支出項目管理：房貸、車貸、信貸、保險、固定生活費、醫療、教育與其他支出
- 每筆手動現金流可新增、編輯、刪除與暫停計入
- 股票／ETF 股息依目前持倉與預估配息自動帶入
- 質押利息與每月本金依借款資料自動帶入
- 每月總收入、總支出與每月自由現金流拆分顯示

V0.5 將手動收入／支出與系統自動帶入的股息、借款成本分開，避免同一筆利息或本金重複輸入。

## V0.6 已完成

- 被動收入目標頁面與月現金流頁面切換
- 預設帶入 00878 示範，也可改選其他股票／ETF 或自訂標的
- 殖利率模式：年度目標 ÷ 殖利率，反推所需本金
- 每股配息模式：輸入 Q1–Q4 配息，反推需要股數與本金
- 淨領目標：把每月借款成本加回毛收入需求
- 目標可保存、套用、更新與刪除，資料只留在本機 IndexedDB

## V0.7 已完成

- 多情境比較：目前基準線與保存方案同表呈現
- 方案設定可保存、套用、更新、複製與刪除
- 比較總資產、淨資產、負債、槓桿、年度股息與每月淨現金流
- 比較 -20%／-30% 市場壓力後維持率與距離追繳跌幅
- 新增年度股息、每月現金流與壓力後維持率的輕量圖表
- 方案只保存設定於 IndexedDB，基準資料更新後會重新計算

## V1.0 已完成

- 本機資料可匯出為帶有 backupVersion 的 JSON 備份檔
- 匯入前先驗證 JSON 結構、版本、設定與資料 ID
- 匯入可選擇依 ID 合併，或以單一 IndexedDB 交易覆蓋本機資料
- PWA manifest、Service Worker、離線啟動、APP icon 與 GitHub Pages workflow 完整保留
- 手機 Bottom Navigation、Responsive layout 與 Light / Dark / System 模式可持續使用

## V1.2 已完成

- 資產管理新增房產分頁，可記錄目前估值、購入價格與租金收入
- 質押模擬新增一般負債分頁，可記錄房貸、車貸、信貸與每月付款
- 房產估值計入總資產，一般負債剩餘本金計入總負債與淨資產
- 房貸與一般負債每月付款自動帶入現金流
- 房產租金收入可自動帶入每月收入
- 固定支出新增捐款與稅費分類，保留手動停用與編輯功能

## V1.1 已完成

- 新增股票時可用股票代號自動查詢台股／美股最新公開行情
- 透過公開讀取代理避開 GitHub Pages 的跨來源限制；只送股票代號，不送持股數、成本或借款資料
- 股票清單顯示自動行情／手動價格與行情時間
- 資產頁上方提供單一「更新所有行情」按鈕，一次批次更新所有台股／美股
- 查詢失敗、限流或其他市場時，保留手動輸入價格的備援流程

V1.1 的行情來自第三方公開資料，可能延遲、限流、格式變更或暫時無法使用；不代表券商成交價、授信價格或金融機構通知。質押與壓力測試前仍應自行核對券商報價。

## V1.3.2 已完成

- 修正美股代號正規化，支援 `AAPL`、`$AAPL`、`NASDAQ:AAPL`、`AAPL.US` 等常見輸入格式
- 將 `BRK.B`、`BF.B` 等美股不同股票類別轉成 Yahoo Finance 使用的 `BRK-B`、`BF-B`
- 切換市場時同步清除舊行情，選擇美股會自動使用 USD，避免沿用台股的 TWD 價格
- 補上美股行情解析與代號轉換回歸測試；「更新所有行情」同樣支援台股與美股

## V1.3.1 已完成

- 負債與質押借款畫面將「目前餘額」改為「剩餘本金（目前尚欠）」，明確表示這是尚未償還的本金
- 新增與編輯視窗補充原始本金、剩餘本金的差異說明，總負債與維持率說明同步改用一致用語

## V1.3.0 已完成

- 股票新增與「更新所有行情」會同步抓取股利事件，優先計算近 12 個月配息
- 近 12 個月沒有股利資料時，退回前一完整年度累計配息
- 依最新股價與每股年配息自動換算年化殖利率，並帶入每月現金流與被動收入試算
- 資產頁顯示配息來源、計算期間與自動／手動狀態；查不到時保留手動備援
- 延續手機表單、下拉選單、切換器與卡片內容的水平溢出修正

## 技術架構

```text
src/
├── domain/       # 財務資料模型與純計算服務
├── data/         # IndexedDB schema、Demo seed、repository
├── components/   # App shell、導覽、共用 UI
├── pages/        # Dashboard、資產、設定與版本邊界頁
├── shared/       # ID 與金額 / 百分比格式化
└── test/         # 測試環境設定
```

所有資產資料只寫入瀏覽器 IndexedDB；目前沒有後端、登入、Analytics 或 Console 財務資料輸出。V1.1 查詢行情時只會把股票代號送往第三方公開行情服務，不會送出持股數、成本、現金、借款或備份內容。未來若要加入 Capacitor，domain 與 data contract 可繼續沿用。

## 本機執行

需要 Node.js 20 以上。

Windows 使用者可以直接雙擊專案根目錄的 `啟動APP.bat`；它會啟動本機 Vite server 並開啟瀏覽器。

```bash
npm install
npm run dev
```

開發伺服器啟動後，使用瀏覽器開啟終端機顯示的 localhost URL。

## 驗證指令

```bash
npm run type-check
npm test
npm run build
npm run preview
```

`npm run build` 會產生 `dist/`、manifest 與 Service Worker。若要在本地模擬 GitHub Pages 專案路徑，可使用：

```powershell
$env:VITE_BASE_PATH = "/your-repository-name/"
npm run build
```

## GitHub Pages 部署

1. 將此資料夾推送到 GitHub Repository 的 `main` 分支。
2. 在 Repository Settings → Pages → Build and deployment 選擇 **GitHub Actions**。
3. `.github/workflows/ci.yml` 會在 push 到 `main` 時執行 type-check、tests、production build，並把 `dist/` 部署到 GitHub Pages。

GitHub Actions 會自動將 `VITE_BASE_PATH` 設為 `/<repository-name>/`，避免專案頁面的資產路徑落到網域根目錄。

## 版本紀錄

### 1.0.0

- 完成本機 JSON 備份匯出與匯入預覽
- 新增合併／覆蓋資料策略與 IndexedDB 覆蓋交易
- 新增備份格式與版本驗證回歸測試
- 更新 V1.0 設定頁、首頁狀態與手機版備份操作

### 1.2.0

- 新增房產資產、一般負債／房貸與月付款模型
- 更新總資產、總負債、淨資產與月現金流計算
- 固定支出加入捐款、稅費分類
- IndexedDB 與 JSON 備份升級至 schema V2，保留 V1 備份匯入相容性

### 1.2.1

- 手機版股票持倉改為一筆一張卡片式清單
- 右側優先顯示持倉市值，並集中呈現持有股數、成本、現價、損益與質押狀態
- 桌面版保留原有表格檢視與行情來源資訊

### 1.2.2

- 台股行情更新時同步查詢 Yahoo 台灣股市中文名稱
- 新增股票與批次更新會優先採用中文名稱，既有英文名稱也能在更新行情時補正
- 保留手動輸入名稱與美股／其他市場的手動備援

### 1.2.3

- 修正手機表單、下拉選單、切換器與卡片內容的水平溢出
- 長股票名稱、長數字與提示文字在手機寬度下自動收縮或換行
- 修正手機頂部列負邊距造成的窄螢幕裁切

### 1.3.0

- 行情查詢擴充為 2 年股利事件資料
- 以近 12 個月配息計算年化每股配息，無近期資料時使用前一完整年度
- 自動依最新股價換算年化殖利率，並更新資產頁、現金流與被動收入目標
- 保留股利期間、更新時間與手動輸入來源，既有 V1.x 資料可直接沿用

### 1.3.2

- 修正常見美股代號格式與 Yahoo Finance 代號的轉換
- 美股行情自動以 USD 儲存與顯示，並補上 AAPL／BRK.B 查價測試

### 1.3.1

- 負債與質押借款統一以「剩餘本金（目前尚欠）」取代容易混淆的「目前餘額」
- 新增與編輯表單補充原始本金與剩餘本金的差異說明

### 1.1.1

- 將行情更新入口收斂為資產頁單一「更新所有行情」按鈕
- 批次更新所有台股／美股並回報成功、失敗與未支援的其他市場

### 1.1.0

- 股票新增／編輯表單接上公開行情查詢與自動帶入
- 股票清單加入行情來源與行情時間標示
- 保留手動價格備援與第三方行情失敗提示

### 0.7.0

- 完成多情境比較與方案保存流程
- 新增方案套用、複製、更新與刪除
- 新增年度股息、每月淨現金流與壓力後維持率圖表

### 0.6.0

- 完成被動收入目標計算器與 00878 預設示範
- 新增殖利率／每股配息／淨領目標三種試算情境
- 新增目標本機保存、套用、更新與刪除

### 0.5.0

- 完成收入／支出項目管理與本機 IndexedDB 儲存
- 完成股息、借款利息、每月本金與自由現金流拆分
- Dashboard 接上每月淨現金流、月股息、月利息與月本金

### 0.4.0

- 完成市場壓力測試與跌幅快速情境
- 新增壓力後維持率、淨資產變化與風險狀態
- 新增警戒線與追繳線的跌幅反推

### 0.3.0

- 完成借款再投入模擬與 Before / After 比較
- 完成槓桿倍數、負債比與新增現金流計算
- 新增模擬擔保品與投入比例控制

### 0.2.0

- 完成質押借款與擔保品管理
- 完成利息、維持率與門檻風控計算
- 首頁接上借款負債與風控狀態
- 新增 repository bundle 回歸測試與手機版水平溢位修正

### 0.1.0

- 建立離線 PWA 基線與 IndexedDB schema
- 完成股票 / 現金資產管理與 Dashboard
- 完成資產公式服務與 9 個單元測試
- 預留 Loan、Collateral、CashFlowItem、Simulation、DividendTarget 與 BackupData 模型

## 隱私與數值原則

- 成本與匯率仍由使用者控制；股價與配息可使用第三方公開行情，查不到時保留手動備援，不代表券商即時成交、除息公告或授信價格。
- 淨資產 = 總資產 − 總負債。
- 借款不是收入；借款再投入只是改變資產組成，未來會同時增加對應負債。
- 任何金融機構的維持率規則都不會在公式層寫死，門檻由設定與未來 simulation use case 提供。
